package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Sophisticated White and Lavender Theme Colors
val DarkBg = Color(0xFFFAF5FF)       // Light lavender background (replacing DarkBg for compatibility)
val DarkCard = Color(0xFFFFFFFF)     // Crisp white surface card (replacing DarkCard for compatibility)
val LightBg = Color(0xFFFAF5FF)      // Clean minimalist off-white background
val LightCard = Color(0xFFFFFFFF)    // Crisp white surface card

val PureBlack = Color(0xFFFFFFFF)    // Light/White container background
val PureWhite = Color(0xFF2E1065)    // Deep, dark rich lavender/indigo text/elements

// Secondary/Border text shades
val TextMutedDark = Color(0xFF7C3AED)  // Muted Lavender for secondary text
val TextMutedLight = Color(0xFF7C3AED) // Muted Lavender for secondary text
val BorderDark = Color(0xFFE9D5FF)     // Soft lavender border
val BorderLight = Color(0xFFE9D5FF)    // Soft lavender border

// Premium Lavender Accents for AI-related interactions
val LavenderAccent = Color(0xFF7C3AED)    // Rich royal lavender
val LavenderLight = Color(0xFF8B5CF6)     // Medium active lavender
val LavenderDark = Color(0xFF5B21B6)      // Deep dark violet
val LavenderBg = Color(0xFFF3E8FF)        // Extremely soft pastel lavender background
val LavenderPill = Color(0xFFEDE9FE)      // Very light soft lavender pill
